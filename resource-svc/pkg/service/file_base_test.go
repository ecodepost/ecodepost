package service

import (
	"testing"
)

func TestGenUploadPath(t *testing.T) {
	// bucket := OssPublicBucket
	//
	// f, p, e := GenUploadPath(commonv1.CMN_UP_TYPE_ARTICLE, bucket, "cmt-xx", "a.jpg")
	// log.Printf("f, p, e--------------->"+"%+v, %+v, %+v\n", f, p, e)
	//
	// f, p, e = GenUploadPath(commonv1.CMN_UP_TYPE_DRIVE_FILE, bucket, "cmt-xx", "a.jpg")
	// log.Printf("f, p, e--------------->"+"%+v, %+v, %+v\n", f, p, e)
}
